---
layout: page
title:  "03calculus3"
author: mosfet
category: math
tags: academic hidden 微积分
---

######
<style>code{color:#c44569}nav a{color:#c44569!important}</style>
<nav style="position:fixed;left:0;bottom:0;background: white;">
[返回](#top)/ [偏微斜度](#mc1)/ [微分多变量函数](#mc2)/ [应用](#mc3)/ [返回](#top)</nav>

注意本文属于数学核心标准(**academic**字样)的沿续。  

---
```
Differential Calculus     不在本文范围
Integral Calculus         不在本文范围
Multivariable calculus    u2✅
```

## 表达式快速参考
```
```

## MC1. 偏微斜度
多变量(multivariable)指的是关于向量格式的函数，即认为拆开每个元都是一个变量。  
`▼`两侧长度任意，含义任意。  

#### 部分/偏微斜度(partial derivative)
∂定义如下：  
```ruby
∂f/dx = limdx->0|[f(x+dx, yz)-f(x, yz)] /dx
```

`▼`给定点p，偏差分x时，物理坐标在x上移动，其f值变动的差异比。具体看法取决于两侧格式。  
例如，f(xy)->h的曲面上，令y=1，∂f/∂x相当于横轴上曲线的斜度，因为df为高度，dx为横轴距离。  
如果f的输出是个单值，根据操作——其他域确定为常数，结果完美对应被差分的那个域上的曲线速率。如果f不为单个值，则可能无法套用此情景。  

`▼`如果存在其他差分值，表达式应写为∂f/∂x，防止dx被消掉，但本质上和普通的dx没什么区别。  

#### 梯度(gradient)
▽定义如下：  
```ruby
▽f = [∂f/∂x, ∂f/∂y, ...]
```
给定一个函数点，输出其所有元的偏微斜度的格式。  

`▼`梯度的解释：您的意思是不是我从A，沿x走到一个点，再回去从y走到一个点，形成两个高度，三个点形成一个平面，那么期间任何一点的高度就由该平面近似，而与**函数本身在该点的值无关**。  
你如何假设这三点之间没有曲面？是因为微分成了平面吗？  

`▼`建议画出图形，令x轴正向，y轴朝右且角度很小，找出此平面来理解。  

`▼`因此，要"预测"某位置的函数值，使用两个已知速率的线性关系。**f(0)+(∂f/∂x)Δx+(∂f/∂y)Δy**。  

#### 方向微斜度(directional derivative)
定义如下：  
```ruby
▽v-f = dot(▽, v)
```
为什么要定义这个？好吧，这就是把各个偏导数和一个方向拆解乘积起来，再获得总和速率。  
这告诉你通过三个已知梯度分值(由函数本身决定)，沿着某个方向的估计速率。同样，这与该函数本身在该方向上某个点的信息无关。  

`▼`除非这两个向量(当函数为单值输出，▽也为向量)重合，否则检查到其他方向的速率小于▽。  

## MC2. 微分多变量函数
#### 参数图形和隐式图形
`F(p) = 0`寻找其约束集代表的图形，数值方法很容易遍历所有位置，评估该点进行测试。  
相反，参数图形仅取决于主动参数，直接精确连续绘制图形。  

#### 微分单参数方程的几何意义
例子：若为单参数，输出位置，结果是两点间的向量，该向量与曲线运动轨迹相切。  

`▼`回顾一下之前的术语可能有助于理解谁影响谁，t↑f(t)↑，或者t↑x↑y↑都可以。  

#### 复杂的参数关系(链式关系)
一种复杂的差分是对f(x,y)，但其域来自一组参数方程，再问你t对f的差分。  

如前所述根据梯度的定义，f的整体差异(∂f/∂x)Δx+(∂f/∂y)Δy，我们再取Δx=【dx/dt】 * Δt。最后整理为：  
```ruby
Δf = [(∂f/∂x)(dx/dt)+(∂f/∂y)(dy/dt)]Δt
Δf/Δt ~= df/dt = dot(▽, [dx/dt, dy/dt])
```

`▼`……这里有一种非常微妙、复杂的区别。和本文的其他结论不同，该结论是一种我的推测，尽管推导经过AI验证，可能无法使用。  
这里有一个示例，就是取实际的f代进去求df/dt，结果当然能出来，而不一定使用此公式，但结果相同。如果你不确定，直接代进去求。  
```
https://www.khanacademy.org/math/multivariable-calculus/multivariable-derivatives/multivariable-chain-rule/v/multivariable-chain-rule
```

## MC3. 应用
#### 曲率(curvature)
曲线上某处的曲率半径是最拟合曲线的一个圆的半径(比其他任何半径都更拟合)，这是测量曲线在每个点的实际弯曲程度的好方法。
如果我们沿着曲线运动，从该点切入到曲率半径的圆上相当于以当时做出的瞬时弯曲程度圆周运动。  
`曲率(curvature)`的定义是该半径的倒数`k = 1/R`。其实际大小从某种速率计算`k = len(limds->0| dtag / ds)`。  
其中，`tag(t)`是所谓的`单位触向量函数(unit tangent vector function)`。这种触向量查询实际上来自于参数方程的微斜度的归一化，即我们已经说明过的参数曲线的速率。该向量会在不同的地方拐弯。  
接下来，`ds`是曲线的微小弧长。方向和该小步长因子结合在一起可以表明你转动的速度。  
```ruby
p(t) = [t-sin(t) 1-cos(t)]

# 计算原始tag函数
tag(t) = normalize(Dpt=[Dft Dgt])
# 接下来根据定义公式，曲率需要用ds对tag差分。这是不可能的，因为ds不直接影响tag(t)

# 而然，
dtag / ds = (dtag/dt) / (ds/dt)
# 如u2.4所述，参数速率是距离(近似ds)除以dt。
          = Dtagt / Dpt
k = len(Dtagt / Dpt)
```

**向量值函数的部分微斜度/参数曲面(U2.7/8)**  
这要求向量值函数至少具有多维输入(因此才可以部分差分！)。  
派生中我们使用与部分差分相同的技巧和原理，每个分量表达式的派生只有当前变量视为变量，而其他变量被视为一些无法评估的常数。  
```ruby
p(t,s) = [fts gts hts]
dp/dt = limdt->0|p(t+dt, s)-p(t,s) / dt

p(t,s) = [t²-s², 5t, ts²-st²]
           #xyz
dp/dt = [2t, 5, 1s²-2st]
```
要解释`dp/dt`时，我们可以考虑参数域平面中的一条原始参考线`s = 2`最后出现在结果参数曲面的位置(一条曲线)，该部分微斜度(方向向量)表示其附近的情况，例如`t+dt`。因此我们确定这是这条曲线上的触向量来表明速率，因为它会导致沿着曲线移动。有关"多维斜度"为向量的解释差异，请见u2.4。  
每个部分微斜度都与曲面该点相触，因为它们来自于多条重合曲线。  

**散度(Divergence)(U2.9/11)**  
**旋度(curl)(U2.10/11)**  
**Laplacian(U2.12)**  
**Jacobian(U2.13)**  

*U3*  
**梯度下降(gradient descent)(U3.4)**  

## MC4. 二重积分
*U4*  
`二重积分(Double integrals)∫∫`(见unit4)是对多变量函数的积分。记为`∫y1..y2|(∫x1..x2|f(x,y)dx)dy`。  

您仍然可以从一维域的曲线下积分进行理解，首先表达式中间的积分称为内积分(组内的积分)，后计算的积分称为外积分。  
对于内积分，它尝试沿着给定的整个x域，对某个y值的平行切片曲线进行积分，这与我们的基本情况完全类似！  
不过，你可能会误以为这个时候需要一个实际的`y`来评估函数`f(x,y)`值，但内积分本身仅根据`dx`进行操作！应该将其展开为表达式考虑。事实就是，内积分是对`dx`的积分，表达式中的y项是对这个积分的常数项，因此该区域倍率可以放到外面(见属性)，从而很容易评估内积分的结果，这将得到一个与y有关的表达式，再积分外表达式，这是有道理的！  
```ruby
∫(∫xy²dx)dy   [0..2 0..1]

df/dx = y²                        # 观察部分微斜度
                                  # 部分积分的处理似乎与部分微斜度相关，可将无关项目视为积分的常数∫cf(x)dx

∫xy²dx = y²∫xdx
       = y²(AD(2) - AD(0))        # x²/2
       = y²(2-0)
                                  # 发现内积分的结果与y有关，这是有道理的(如上)，取决于选择哪条曲线
                                  # 内积分的过程本身与y无关，而只和x有关。而y的输入导致了可变的内积分结果
```
将顺序改为先计算y，结果一致。你可以从图形中获取类似的代替理解。  

如果不仔细进行分步骤积分而查看粗略结果，可以获得另一种观点。就其整合而言，由于函数的提升，现在摄取曲线高度扩展为曲面高度`h = f(x,y)`，细分间隔也将匹配更高的域`dA = dxdy`。记为`(∫+)Domain|fdμ`。其中`μ = dxdy...`与域的最小划分匹配。一般实际问题可能会用这种表达式，但本质、和评估上没有差别。  

一种常见的情况是，域不为固定边界，即在内积分时`x`的间隔可能依赖于y，这导致域来自于`x = ?y`的相关量，此时y还无关吗？实际上仍然无关，因为不影响内积分过程。画出该函数更容易理解该过程指定的四个边界。  
评估这种内积分仍然很简单，只是AD时会评估带有y的变量而已。  
```ruby
   |y|
  /  |     ∫x=sqrt(y)..x=1
/    |     ∫y=0..1
------x
```

**曲面积分(见外部)**  

**通量(见外部)**  

---
## 通用翻译
```
偏微斜度|partial derivative
梯度|gradient
方向微斜度|directional derivative
```